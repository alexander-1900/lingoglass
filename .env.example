# Optional: Japanese tokenizer microservice (services/sudachi).
# Unset = built-in regex fallback (no readings/romaji from Sudachi).
SUDACHI_SERVICE_URL=http://localhost:8300/

# Optional: canonical URL used for absolute links in sitemap.xml
SITE_URL=http://localhost:3000
