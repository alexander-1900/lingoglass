# Japanese tokenization runs natively: services/sudachi/sudachi_tokenize.py is
# spawned via child_process. Install its deps once (see requirements.txt);
# set PYTHON if your interpreter is not on PATH as `python`/`python3`.
# PYTHON=C:\\Python314\\python.exe

# Optional: canonical URL used for absolute links in sitemap.xml
SITE_URL=http://localhost:3000
